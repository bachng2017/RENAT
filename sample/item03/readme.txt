The sample requires:
    - a running Juniper router
    - a running Ixia Network TCL server
Results are `result/best4.xlsx` and `result/best6.xlsx`

Modify those information to run the sample
1. device information in $RENAT_PATH/config/device.yaml `vmx11` and `ixnet03_8009` section
2. account information in $RENAT_PATH/config/auth.yaml `default` section

