The sample requires a running router.
Commands are define in config/cmd.lst and config/cmd2.lst

Modify those information to run the sample
1. device information in $RENAT_PATH/config/device.yaml `vmx11` section
2. account information in $RENAT_PATH/config/auth.yaml `default` section


